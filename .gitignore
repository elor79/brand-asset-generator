node_modules/
dist/
.env
.env.local
.DS_Store
vite.config.js.timestamp-*.mjs
*.log
# Scratch probes. The mount forbids unlink(), so throwaway files can only be
# renamed aside, never deleted — they must not be allowed to drift into a commit.
*.scratch-*
*.safetensors

# playwright artifacts
test-results/
playwright-report/
